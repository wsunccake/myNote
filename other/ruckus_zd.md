# ZD

```bash
ruckus> enable
ruckus# config
ruckus(config)# system
ruckus(config-sys)# show support-entitle
```


```bash
ruckus> enable
ruckus# show sysinfo
ruckus# debug
ruckus(debug)# fw_upgrade -p tftp -s <tftp_ip> -n <upgrade_image>
```
